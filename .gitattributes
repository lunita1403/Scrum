import React, { useState } from 'react';

const AuthLoginScreen = ({ onLoginSuccess, onGoToRegister }) => {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');

  const handleLogin = () => {
    setError('');
    // La validación ahora es más flexible para que siempre puedas iniciar sesión
    // Puedes cambiar 'usuario' y 'password' por los valores que desees para una validación real
    if (username && password) { // Si ambos campos tienen algo, se considera éxito
      onLoginSuccess('movies'); // Cambiado a 'movies' para ir al dashboard de películas
    } else {
      setError('Por favor, ingresa un usuario y una contraseña. ¡No te hagas el despistado!');
    }
  };

  return (
    <div className="relative min-h-screen flex items-center justify-center p-4 overflow-hidden">
      {/* Fondo de cine con efecto de luz */}
      <div className="absolute inset-0 bg-cover bg-center" style={{ backgroundImage: 'url("https://images.unsplash.com/photo-1579547621244-0401a67a177d?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D")' }}>
        <div className="absolute inset-0 bg-black opacity-70"></div>
        <div className="absolute inset-0 flex items-center justify-center">
          <div className="w-3/4 h-3/4 bg-gradient-to-br from-red-600 to-purple-600 opacity-30 rounded-full filter blur-3xl animate-pulse"></div>
        </div>
      </div>

      {/* Contenedor del formulario */}
      <div className="relative bg-white bg-opacity-10 backdrop-filter backdrop-blur-lg p-8 rounded-2xl shadow-2xl border border-gray-700 max-w-md w-full transform transition-all duration-500 hover:scale-105">
        <h2 className="text-4xl font-bold text-white text-center mb-8 drop-shadow-lg">
          ¡Bienvenido al Cine!
        </h2>
        <div className="mb-6">
          <input
            type="text"
            placeholder="Usuario"
            className="w-full px-5 py-3 bg-white bg-opacity-20 border border-gray-600 rounded-xl text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-red-500 transition-all duration-300 text-lg"
            value={username}
            onChange={(e) => setUsername(e.target.value)}
          />
        </div>
        <div className="mb-8">
          <input
            type="password"
            placeholder="Contraseña"
            className="w-full px-5 py-3 bg-white bg-opacity-20 border border-gray-600 rounded-xl text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-red-500 transition-all duration-300 text-lg"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
          />
        </div>
        {error && (
          <p className="text-red-400 text-center mb-6 text-sm font-medium animate-bounce">{error}</p>
        )}
        <button
          onClick={handleLogin}
          className="w-full bg-gradient-to-r from-red-600 to-purple-600 text-white py-3 rounded-xl text-xl font-semibold shadow-lg hover:from-red-700 hover:to-purple-700 transition-all duration-300 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 focus:ring-offset-gray-900"
        >
          Entrar
        </button>
        <p className="text-gray-400 text-center mt-6 text-sm">
          ¿No tienes cuenta?{' '}
          <button onClick={onGoToRegister} className="text-red-400 hover:underline focus:outline-none">
            Regístrate aquí
          </button>
        </p>
      </div>
    </div>
  );
};

export default AuthLoginScreen;

// DONE# Auto detect text files and perform LF normalization
* text=auto
